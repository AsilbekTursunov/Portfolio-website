'use client'

import MainWrap from '@/src/components/MainWrap'

const MainPage = () => {
	return (
		<main className='h-screen bg-primaryDark w-screen min-w-full max-lg:p-5 flex mx-auto lg:items-center lg:justify-center'>
			<MainWrap />
		</main>
	)
}

export default MainPage
